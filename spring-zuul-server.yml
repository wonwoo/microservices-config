info:
  component: Zuul Server modify test

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true

server:
  port: 8765

zuul:
  routes:
    users: /users/**

