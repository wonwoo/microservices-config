info:
  component: Zuul Server

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: true

server:
  port: 8765

zuul:
  routes:
    users: /users/**
    board : /board/**

